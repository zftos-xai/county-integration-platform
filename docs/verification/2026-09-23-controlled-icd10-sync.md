# 2026-09-23 ICD-10 受控真实同步核验

## 验收合同

- **目标**：在测试环境执行一次受控 ICD-10 公共目录同步，并核对目录数量、批次详情、HIS 调用记录和管理审计记录。
- **入口**：管理端 `http://127.0.0.1:5173`，批次页 `/master-data/batches`。
- **主体与权限**：`admin` 平台管理员；仅使用已授权的青神县汉阳镇中心卫生院作为 HIS 调用端点，不将机构归属写入公共目录。
- **业务范围**：ICD-10 诊断目录，测试环境，指定时间范围 2000-01-01 至 2026-09-23 16:37（本地时间），交易 `100-007` 数量查询和 `100-006` 分页查询。
- **允许写操作**：只允许创建并执行一个同步批次；若执行中断，只允许使用页面的受控收尾，不重复提交、不手工修改业务表、不修改 Flyway 历史。
- **安全边界**：只核对脱敏请求/响应摘要、数量、终态、批次和审计编号；不保存或输出完整 SOAP、端点地址、凭证或密码。

## 实际执行

| 项目 | 结果 |
| --- | --- |
| 批次号 | `BD-7DCE31F6CAE4495281D1` |
| 发起时间 | 2026/09/23 16:37:35（管理端显示） |
| 受控收尾时间 | 2026/09/23 16:58:12（管理端显示） |
| 总耗时 | 20分36秒 |
| 最终状态 | 存在未知结果 |
| 目录范围 | 平台公共目录；未写入机构归属 |
| 执行范围 | 西医诊断、中医诊断 |
| HIS调用留痕 | 368 次，均在批次详情的“HIS 请求记录”分区可读 |

## 核对结果

### 1. 目录数量

`/master-data/directory/icd10` 回读到当前已发布目录：

- 西医诊断：36,647 条。
- 中医诊断：2,653 条。
- 最近记录批次仍为已完成批次 `BD-A361C02C146342A88F7D`，不是本次未知批次。

结论：本次中断批次未覆盖当前有效目录，上一成功目录保持不变。

### 2. 批次详情

批次详情显示：

- `100-007` 西医数量查询成功，结果码 `1`，返回/声明 36,647 条。
- `100-006` 西医分页调用已保存并持续成功；中医分项尚未开始。
- 受控收尾后显示“存在未知结果”，没有伪造西医/中医最终对账结果。

### 3. HIS调用记录

批次详情的“HIS 请求记录”显示 368 次调用，包含交易码、诊断类别、分页范围、结果码、返回数量、耗时及脱敏请求/响应摘要。首条记录为西医 `100-007` 数量查询（36,647 条，329ms），后续为西医 `100-006` 分页记录。未保存完整 SOAP 报文、端点或凭证。

通用 `/exchanges` 页仍展示既有通用交换事实；本次 ICD-10 分页事实以批次详情的专用留痕为准。

### 4. 管理审计

审计页已核对本批次的四条关键记录：

- 创建批次：成功，创建公共 ICD-10 批次。
- 开始同步：成功，请求编号 `b6e227e4-05fa-40e8-b1a6-896f4892a02c`。
- `MASTER_DATA_ICD10_SYNCED`：失败，已保存分项 0 类、新增 0 条。
- `MASTER_DATA_BATCH_RECOVERED`：成功，请求编号 `b7e4365b-195b-4a20-9c56-01fcf86411fc`；说明为已结束中断同步、保留已保存事实且未重新调用 HIS。

## 执行中发现并修复的问题

批次详情首次读取因 `md_icd10_his_invocation.diagnosis_category` 存储值为 `0/1`，而 MyBatis 构造映射按枚举名称读取，导致接口报错。已在 `Icd10SyncMapper.xml` 的 HIS 调用查询中显式映射 `0→WESTERN`、`1→TRADITIONAL`，重启后详情正常回读 368 条调用记录。

详情面板的旧接口时间值未携带时区后缀，前端曾按本地时间解析而比批次概要少 8 小时；已统一按服务端 UTC 值补齐时区后缀再格式化，批次概要、HIS 调用记录和审计记录现均显示一致的北京时间。

## 自动化复验

- `./tools/verify.sh`：通过。
- 后端测试：313 项，失败 0，跳过 6 项未显式启用的真实端点探测。
- 前端测试：164 项，失败 0；类型检查、生产构建、SQL迁移静态检查通过。

## 结论与后续

本次受控真实同步链路、持久化和留痕核对完成，但业务验收未通过：中医分项未取得，批次为结果未知，当前目录未改变。下一步应在不重复使用该批次的前提下，由环境/业务负责人确认端点稳定性和分页策略后，另建批次完成两类分项，并再次核对数量、批次详情、HIS调用和审计。
