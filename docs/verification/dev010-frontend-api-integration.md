# DEV-010 前端真实API接入记录

## 当前已完成的页面到数据库功能

管理端已接入真实会话、机构、用户、角色权限、参数与字典接口：CSRF令牌、服务端Session登录、当前用户、首次登录改密、退出，以及机构、用户、授权范围、角色、代码注册权限、按适用范围保存的参数值、字典类型和字典项管理。`/prototype`继续作为独立合成演示入口；正式管理入口不使用演示账号，也不使用`localStorage`代替服务端会话。

## 已实现边界

- 写请求先读取`/api/v1/session/csrf`，并使用服务端返回的请求头名称和令牌。
- 所有请求使用同源会话凭据；密码、Session标识和CSRF令牌不写入本地存储。
- 当前用户的权限代码控制菜单可见性；直接URL访问使用同一权限规则再次检查。
- `mustChangePassword`为真的用户只能进入修改密码页面；修改成功后按后端接口规则注销并重新登录。
- 400、401、403、404、409、500、网络失败和非平台CSRF响应使用受控文案；存在`requestId`时展示以便定位。
- 后端仍是最终授权边界，前端权限判断不推导机构范围或扩大权限。
- 机构列表完全使用后端返回的数据范围；新增机构必须选择可访问的启用父机构，机构类型只接受项目已确认的代码，不在前端编造枚举。
- 机构修改和启停回传当前Base64 `rowversion`；409时保留编辑内容并提供刷新最新详情，启停操作先刷新列表再显示冲突。
- 机构页覆盖加载、筛选无结果、范围内空数据、只读权限、403、409、网络失败和`requestId`展示；会话过期返回登录页。
- 新增机构会改变当前账号的显式机构范围，成功后前端主动清除旧用户信息并返回登录页，明确提示重新登录加载最新权限，避免旧Session在下一次请求时突然失效。
- 用户页分别维护基础资料、角色、机构范围和临时密码草稿；当前账号权限、范围、状态或密码变化后主动结束旧会话并清除用户信息并要求重新登录。
- 角色页只展示后端代码注册权限；系统保护角色不可编辑，非系统角色的资料更新携带Base64 `rowversion`，权限替换不允许提交未注册代码。
- 参数页只呈现后端代码注册定义，不提供任意键编辑器；环境、适用机构、类型和数值/格式约束由定义驱动，敏感已有值不回填表单。
- 字典页以类型和字典项分层维护平台自身字典；稳定代码创建后只读，类型和字典项更新均携带Base64 `rowversion`。

## 验证结果

- 后端改用`18080`启动，Vite管理端在`5173`通过同源代理连接`http://127.0.0.1:18080`；不再依赖已被其他服务占用的`8080`。
- SQL Server 2012实例成功连接，Flyway验证V0100至V0500共5个主版本；应用健康检查返回`UP`。
- 在真实Spring Boot、SQL Server和Vite代理访问过程完成一次性初始化、首次登录、强制改密、重新登录和Session续用。
- 在同一真实数据传递过程完成机构列表、详情、创建、修改、停用、重新登录后读取停用机构、再次启用；最终根机构和联调机构均为启用状态。
- 使用过期`rowversion`重复提交机构修改，首次请求返回200，第二次返回409及`RESOURCE_CONFLICT`，并发控制检查通过。
- 真库联调发现并修复SQL Server可空时间参数缺少`jdbcType`、MyBatis构造器基本类型不匹配，以及停用机构从显式管理范围消失而无法重新启用的问题；新增Mapper XML回归测试防止复发。
- 后端Maven测试69项、前端自动化测试56项全部通过；Web Admin TypeScript检查和生产构建通过。
- Codex in-app browser已确认正式登录页可在`http://127.0.0.1:5173/login`正常渲染。
- Chrome真实管理员会话已确认角色列表、9项后端注册权限、系统角色只读保护、抽屉焦点进入、Escape关闭和焦点恢复；本轮未执行角色写入。
- Chrome真实管理员会话已确认参数与字典初始空态、正式侧栏入口、1445及1280桌面布局无横向溢出、字典新增抽屉几何与焦点恢复；浏览器控制台无错误或警告。后续已建立默认配置，需按本轮VISTA记录追加有数据状态复验。
- 本机开发库已回填6条按适用范围保存的参数值、3个字典类型和8个字典项；管理员会话逐条创建字典并刷新回读成功。期间发现配置保存成功时调用通用关闭方法会被`isSaving`保护拦截，已改为成功分支直接清理编辑状态并增加自动化回归测试。

## 仍待验证项

- 尚未使用独立只读用户完成按钮隐藏与写请求403验证。
- 尚未构造第二棵机构树完成跨机构读取和写入拒绝验证。
- 管理员机构写入和并发冲突已通过真实同源API数据传递过程验证，但尚未补充自动化浏览器表单操作用例。
- 当前使用本地开发数据库和信任服务器证书设置，不能替代医院环境中的SQL Server补丁级别、正式证书、`Secure` Cookie、最小权限账号、备份恢复及安全复核。
- 用户和角色页面当前完成真实读取与界面验证，尚未执行“写入可回退测试数据、刷新页面确认写入结果、查询审计记录、清理测试数据”这组验证步骤。
- 参数与字典页面已具备批准的最小默认配置；既有空态记录仅代表默认配置建立前的历史状态，有数据状态、管理员修改、审计回读和恢复默认值以本轮VISTA验收记录为准。
- 字典有数据状态已完成真实写入和刷新回读；保存后自动关闭修复已通过工程测试，但浏览器连接在定向复验时中断，因此该交互的local-ui复验仍标记为`partial`。参数定义需要后端加载新代码后再完成正式页面有数据状态复验。
- 下一项需要从页面到数据库完整验证的功能是外部系统与环境服务地址管理。
