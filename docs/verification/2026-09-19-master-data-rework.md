# 2026-09-19基础数据返工与真实联调记录（历史记录，已废止）

> 2026-09-19 复核后，本记录所依据的“机构映射、人工复核、人工发布”业务模型已撤回。记录中的接口调用、页面和测试结论不能作为基础数据业务完成或验收证据；后续以[基础数据业务详细计划 2.0](../plans/基础数据业务详细计划.md)重新验收。最新真实联调证据见[2026-09-20医院综合目录真实同步验证](2026-09-20-hospital-directory-live-sync.md)。

## 结论

本轮未通过100-003科室业务最终验收。已完成安全闸门、100-008映射代码、批次暂存/问题页面代码，并取得一批可发布的真实科室暂存数据；尚无人工发布、正式表回读、HIS只读读取或三方数量核对证据。

## 已确认事实

1. 旧批次`BD-C59DE736E0F34589BBFC`没有保存`100-008`来源机构ID，却以平台机构范围接收了439条100-003数据，其中438条进入暂存、1条校验失败。由于来源范围无法确认，该批次已经关闭为`RECHECK_REQUIRED`，不得发布。
2. 后端现在要求每个机构批次固化唯一、启用且在同一运行环境通过验证的来源机构ID。没有映射、跨环境复用映射、存在多条映射、映射被停用、批次快照与当前映射不一致时，创建、取得或发布都会拒绝。
3. 测试环境100-008不带名称条件时真实返回179个机构。此前按平台名称模糊查询得到的`03CDD57CE12F4720BB73AB71A944A036`、“青神县汉阳镇中心卫生院22222”不是本机构，不能建立映射。
4. 100-008返回了与平台机构编码完全一致的来源机构：`8BCDCA4E11A44A9D888D7E70DD4FF44F`，名称“青神汉阳镇中心卫生院”。映射发现改为优先使用同编码结果，确认时重新查询并校验来源ID与名称；同范围旧映射保留历史但自动停用。
5. 使用当前有效机构凭证和正确来源机构ID调用100-003成功返回46条科室。干净批次`BD-DB6D2B0D024D4DC9B884`状态为`READY_TO_PUBLISH`，取得46条、无效0条、冲突0条；尚未代替业务人员执行发布。
6. 使用错误来源机构ID调用时，HIS提示“无权访问……的数据或者授权已过期”。该提示是机构范围校验结果，不是当前凭证失效证据。

## 已实现但尚未业务验收

- 100-008实时发现候选项、确认时再次查询、映射保存和验证批次留痕。
- 同步批次固化来源机构ID并在创建、取得、发布三个入口复核。
- 科室暂存数据分页查看、问题详情、核查结论登记；最后一条问题完成后关闭旧批次并要求重新取得，不允许直接发布。
- 正式管理端机构映射页和批次复核页。

## 自动化检查证据

2026-09-19分项执行检查结果：

- Java Checkstyle、Java 17构建和155项后端测试通过；
- 前端注释和10个正式菜单交付检查通过；
- 管理端与嵌入端TypeScript严格类型检查、生产构建通过；
- 114项前端自动化测试通过；
- 仓库统一入口尚未通过：并行新增的`V0700__database_contract_maintenance_main.sql`缺少项目要求的迁移头、表说明和可落库扩展属性，SQL静态检查已拒绝该脚本；本轮不得声称统一检查全部通过。

上述分项结果不替代100-003业务验收。当前开发服务为避免未迁移的V0700对象阻断本轮页面复核，临时关闭数据库契约启动门禁；该实例不能作为数据库结构兼容性验证证据。

## 尚未完成项和下一步

当前凭证和正确来源机构ID已经通过100-003真实调用，不再把“开通授权”列为阻塞项。下一步由业务人员复核46条暂存数据并人工发布，再完成正式科室回读、HIS只读读取和来源/平台/HIS数量一致验证。完成这些步骤前只能标记“待发布/待验收”，不得标记为业务已完成。
