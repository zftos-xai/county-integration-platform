# ICD-10 开发库迁移与真实来源同步核查（2026-09-22）

## 迁移历史与结构

- 开发库 `flyway_schema_history` 已执行 V0100–V1102。工作区有 11 个已执行迁移被修改；逐个计算 Flyway CRC32 后，仓库 `HEAD` 内容与数据库记录完全一致。已将这 11 个文件恢复为已执行内容，未运行 `flyway repair`。原工作区差异另存本机 `/tmp/cip-migration-history-20260922-175128.patch`。
- 以开启迁移和数据库契约门禁的后端启动，V1200、V1201、V1202 均成功执行，数据库版本到 V1202。V1202 只补写既有 V0600 端点校验对象的 `MS_Description`，保留原迁移校验和。
- 实例为 SQL Server 2012 `11.0.7507.2`，数据库兼容级别 110。启动日志报告 `[DB-CONTRACT] PASSED`，核对 348 个字段及全部构造映射；`md_icd10_diagnosis` 和 `md_icd10_sync_result` 两表存在。

## 真实 HIS 取数结果

获授权管理员使用 `TEST` 环境已验证端点，通过正式批次 API 请求西医和中医两类，查询窗口为北京时间 2000-01-01 00:00:00 至批次创建时刻。此窗口不证明覆盖了 HIS 的全部历史记录。

| 批次 | 结果 | 事实 |
| --- | --- | --- |
| `#7` | 已收尾为 `COMPLETED_WITH_UNKNOWN` | 首次分页发现请求 1–100 行只返回 99 条，未写入目录。原结果回读还发现数据库类别 `0/1` 与 Java 枚举名映射不一致，已修复。 |
| `#8` | `COMPLETED_WITH_UNKNOWN` | 复核计数：西医 36,647 条、中医 2,653 条；两类第一页均是请求 1–100 行、返回 99 条。未写入目录。 |
| `#9` | `FAILED` | 调整分页结束行后，构建产物仍使用旧请求校验代码，客户端未向 HIS 发送分页请求。清理并重编译后已恢复。 |
| `#10`（`BD-AC2D7150EE834D57B748`） | `FAILED` | 100-007 声明合计 39,300 条，100-006 逐页实际取得 39,300 条。西医 36,647 条中有 835 条字段不满足必填或文档长度规则；中医 2,653 条中有 42 条重复、538 组同类别同编码冲突。两类均未通过完整性校验，因此未发布当前目录。 |

批次 `#10` 完成后直接查询 `SELECT COUNT(*) FROM dbo.md_icd10_diagnosis` 返回 **0**。这些数值来自真实 `TEST` 端点及开发库回读，未保存 HIS 原始报文、验证码或病种正文。下一步须由来源方和业务方核定字段越界、重复及同编码冲突的处理规则，再以新批次重新同步；不得直接跳过校验并宣称已拉取正式目录。

## 自动检查

- `mvn -f backend/pom.xml clean test` 通过；包含 ICD-10 排他结束行分页和缺页拒绝测试。
- `node tools/verify-sql-migrations.mjs` 通过；数据库契约生成脚本与迁移一致。
- 完整 `./tools/verify.sh` 通过：后端 Maven 构建与测试、SQL 迁移检查、前端注释、类型检查、157 项前端测试和生产构建均通过。
