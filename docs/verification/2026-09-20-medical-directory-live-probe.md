# 100-004/100-005 医院目录真实探测

## 结论

2026-09-20 使用测试机构已保存的基层 HIS 配置完成 `100-005` 四类声明行数查询，并读取 `100-004` 四类第一页数据。该证据仅确认协议参数、授权和第一页字段结构可用，不代表已经完成全量分页、平台保存、自动发布或业务验收。

## 调用范围

| 项目 | 实际值 |
| --- | --- |
| 平台机构 | 青神县汉阳镇中心卫生院 |
| 平台机构编码 | `8BCDCA4E11A44A9D888D7E70DD4FF44F` |
| 环境 | 测试环境 |
| 约定时间范围 | `2000-01-01 00:00:00` 至 `2099-12-31 23:59:59` |
| 数量交易 | `100-005` |
| 数据交易 | `100-004` |
| 第一页范围 | 第 1 至第 100 行；耗材按声明量读取第 1 至第 86 行 |

约定时间范围目前只是联调输入。接口资料未明确其过滤的是创建时间、更新时间还是其他来源时间，也未说明边界、时区及迟到数据处理，因此不能把本次结果称为“全量目录”。

## 真实返回摘要

| 目录类型 | 100-005 声明行数 | 100-004 第一页返回 | 第一页必填缺失 |
| --- | ---: | ---: | ---: |
| 中药 | 512 | 100 | 0 |
| 西药 | 679 | 100 | 0 |
| 诊疗 | 12106 | 100 | 0 |
| 耗材 | 86 | 86 | 0 |

必填缺失只核对接口资料明确要求的目录编码、目录名称、目录类别名称和来源创建时间；没有据此推断停用状态或来源更新时间。

耗材已进一步按第 1 至第 86 行完成完整读取：`100-005` 声明数为86，`100-004` 实际返回86条，稳定目录编码无重复；对同一批内存记录执行的声明数、必填字段和编码冲突校验也已通过。该结果确认当前 HIS 的行号从1开始且结束行包含在请求范围内；中药、西药、诊疗尚未完成全部页读取和跨页核对。

2026-09-20 15:31 的后续只读探测中，中药和西药仍成功，诊疗的 `100-005` 行数查询在15秒后超时。该次结果按“结果未知”处理，没有立即自动重发，不能用早先一次成功结果替代本次未知结果。

## 实际兼容事实

1. 当前部署的 `100-005` 使用平台机构编码可以成功查询；使用最近一次 `100-008` 返回的来源机构 ID 时，HIS 明确返回无权访问或授权已过期。实现不得假定一个机构参数适用于全部交易。
2. 接口资料将目录名称标为可选，但该 HIS 在 JSON 中完全缺少“目录名称”键时返回内部空引用错误；保留该键并传空字符串后调用成功。
3. `100-004` 返回中的转换系数和是否启用可能使用数字或布尔标量，协议适配器已统一转换为受控文本，业务层不接收通用 JSON。
4. 本次日志只保留交易名称、交易码、机构、环境、结果和耗时，没有输出服务地址、验证码或目录正文。

## 尚未完成

- 按固定页大小完整读取全部页，并证明没有漏页、重页或越界页；
- 按目录编码执行跨页去重和冲突校验；
- 证明所有成功页的实际返回总数等于同范围 `100-005` 声明行数；
- 明确“是否启用”的真实取值范围和停用处理，未确认前不得因本批缺失自动停用旧数据；
- 建立内部暂存、正式目录、批次摘要和短事务原子更新；
- 完成正式数据回读、消费方读取和业务负责人验收。

因此当前状态应写为“真实数量查询和第一页读取已通过，完整同步与发布未完成”。
