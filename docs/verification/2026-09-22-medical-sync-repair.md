# 医疗目录同步链路修复与验收

## 编码前契约

- 范围：管理端医疗目录批次创建、执行、数量查询、分页、校验、按类型落库、结果汇总、交换记录和页面回读。保留工作区已有改动及历史失败批次。
- 事实依据：`2026-09-20-medical-directory-live-probe.md` 的真实联调说明当前部署使用平台机构编码；100-008 的返回 ID 仅作配置身份确认，不能代替交易机构参数。
- 保留 `start(request, actor)`、`run(id, version, actor)` 和 HTTP 契约；机构授权仍在 Controller，按 ID 的机构范围仍进入 SQL。定时任务使用批次快照，不伪造认证用户。
- 将内部 `fetchAll(organizationId, environment, type, start, end, sourceCode)` 收敛为 `fetchAll(batch, type)`，复用已有快照表达调用范围及交换记录关联，不新增重复 DTO。
- 每次 100-005/100-004 调用通过现有 ExchangeRuntimeRecordService 保存独立请求号、批次号、类型、分页范围、结果码和受控分类。不得保存凭证、地址、完整报文或任意上游异常正文。
- 全部明确失败归为 FAILED；部分成功/失败归为 COMPLETED_WITH_ERRORS；未知保留明确的未知状态和禁止自动重发语义。数据库已有状态及约束复用，不修改迁移历史。
- 越权不得发起 HIS；旧版本/重复运行在调用前失败；任一页失败、缺页、重复编码、数量不一致均不得提交该类型；其他成功类型可独立提交。
- 查询时间在 API/数据库按 UTC 保存，调用不带时区的 HIS 协议时必须显式转换为医院时间；当前医院按 Asia/Shanghai 配置。该转换不代表确认了 HIS 过滤字段或全量快照语义。

## 验收边界

用户已明确允许使用青神县汉阳镇中心卫生院测试环境 HIS 配置，写入现有开发库，保留新批次、目录、交换记录和审计；不修改 HIS、不重放旧批次、不篡改历史事实。

真实页面合同及逐项证据保存在 `/tmp/county-sync-acceptance-mmJyCg`。工程测试、local-ui、真实查询与实际持久化分别记录，尚未执行的项目不得标为通过。

## 首次失败证据

- 用户截图批次 BD-C5C59283325D49D8B6A7（开发库 id=4）：四类均 FAILED、声明数 NULL，返回/新增/更新均零，汇总却是 COMPLETED_WITH_ERRORS。
- 该批次保存的 HIS 机构参数不同于平台机构编码。
- 现有 fetch 层替换上游错误为固定异常，sync 层再次替换；该时段未查询到 100-004/100-005 交换记录。
- 独立探测测试绕过批次创建和参数固化，不能证明页面同步链路正确。

## 真实页面验收结果

2026-09-22，管理员在 `http://127.0.0.1:5173/master-data/batches` 新建且只运行一次批次 **BD-E8E269A41A24423C9736（id=5）**。机构、测试环境与原失败批次一致；输入时间为北京时间 2020-01-01 09:15 至 2026-09-22 09:15。

| 目录 | HIS 声明 | 分页返回 | 实际新增 | 当前目录回读 |
| --- | ---: | ---: | ---: | ---: |
| 中药 | 54 | 54 | 54 | 54 |
| 西药 | 73 | 73 | 73 | 73 |
| 诊疗 | 419 | 419 | 419 | 419 |
| 耗材 | 9 | 9 | 9 | 9 |
| 合计 | 555 | 555 | 555 | 555 |

- 批次终态 `COMPLETED`，页面显示耗时 30 秒；更新、无效、重复、冲突均为零。
- SQL 独立回读确认四类分项和目录持久化数量一致，业务唯一键重复数为零。
- 100-005 四次、100-004 八次，合计 12 条成功交换记录，均关联本批次；创建和同步完成各一条成功审计。
- 请求超过前端等待时间后，页面实际进入“后台执行、自动读取进度”；随后自动显示完成状态并刷新列表，没有重发运行请求。
- 整页刷新后再次查看批次详情，再分别打开四类目录，数量及来源批次一致；修复详情页跳转目录遗漏机构参数，并在浏览器复验 URL 与当前机构。
- 保留新目录、批次、交换和审计记录；历史 id=4 失败批次未覆盖，旧的“部分未完成”状态没有伪造为新结果。

## 工程验证

- `./tools/verify.sh` 最终通过：后端 277 项，失败 0、错误 0、跳过 2；前端 132 项通过，类型检查、注释规则和两端生产构建通过。跳过的真实 HIS 测试不作为成功证据，上述页面同步另行提供真实证据。
- 新增批次创建→真实 Service 编排→取数/校验→Mapper 的组合回归测试；HIS 和数据库为 mock，明确不冒充真实联调。
- 补充机构参数、北京时间转换、错误追踪、超时不重发、短页/重复阻断、全失败汇总、零行更新不能算成功的回归。
- 运行时数据库启动门禁通过：SQL Server 2012、兼容级别 110、314 个字段及构造映射一致；未改迁移历史。
- 启动与全量验证曾因同时编译同一 target 发生构建竞争；后改为验证结束后串行启动，最终服务和验收使用成功启动版本，不将失败尝试当作通过。

## 证据与未覆盖范围

使用 `vista-web-acceptance` 将工程检查、真实页面操作、SQL 回读分开记录；原始脱敏日志及合同位于 `/tmp/county-sync-acceptance-mmJyCg`，逐项报告为 `report.json`。

- 最终工程日志：`verify-accepted.log`；SQL 回读：`db-accepted.log`；运行日志：`backend-acceptance-runtime.log`；浏览器操作摘要：`ui-observations.md`。
- 实测为当前浏览器 755×908 CSS 像素、DPR 2；盘点了列表、分页/末端、新建弹层、结果抽屉及相关详情/目录。没有宣称完整跨设备、主题对比度或全角色 UI 验收通过。
- 本批次证明**指定时间范围**的真实查询、完整性校验与持久化闭环，不证明来源历史全量。HIS 时间过滤字段含义仍待接口方确认；未返回的旧目录不得据此失效。
- 错误、超时、重复等反例以回归测试覆盖；没有为了测试而破坏真实 HIS 或伪造失败数据。生产发布、物理设备认证、全系统业务验收未执行。
- 凭据仅用于当前登录，未写入代码或验收文件；未提交、推送代码。
