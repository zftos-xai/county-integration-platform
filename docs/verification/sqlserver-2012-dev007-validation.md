# SQL Server 2012 DEV-007真库验证记录

## 1. 验证结论

2026-09-16在项目测试实例完成DEV-007升级迁移和数据库约束验证。实例为Microsoft SQL Server 2012 SP4 Express 64位，产品版本`11.0.7001.0`；`county_integration`数据库在线，兼容级别为110。

V0400已从既有V0100至V0300基线成功升级，迁移后Flyway对4个版本的校验全部通过。参数值、系统字典类型和系统字典项三张表已建立，表、字段、约束及索引的`MS_Description`检查通过。

## 2. 已执行验证

| 验证项 | 结果 | 说明 |
| --- | --- | --- |
| SQL Server版本 | 通过 | SQL Server 2012 SP4 Express 64位，`11.0.7001.0` |
| 数据库兼容级别 | 通过 | `county_integration`兼容级别110 |
| 既有迁移一致性 | 通过 | V0100、V0200和V0300校验和与当前迁移一致 |
| V0400升级迁移 | 通过 | `configuration main`执行成功 |
| Flyway迁移后校验 | 通过 | 成功校验V0100至V0400共4个迁移 |
| 表和字段中文注释 | 通过 | 基线脚本未发现缺少`MS_Description`的业务表或字段 |
| 字典类型代码唯一约束 | 通过 | 事务内重复写入被拒绝 |
| 字典项代码唯一约束 | 通过 | 同一字典类型内重复写入被拒绝 |
| 字典项排序范围约束 | 通过 | `sort_order=1000000`被拒绝 |
| 参数作用域唯一约束 | 通过 | 同参数、环境和全局作用域的重复写入被拒绝 |
| `rowversion`并发控制 | 通过 | 有效版本更新成功，旧版本再次更新影响0行 |
| 验证数据清理 | 通过 | 所有行为验证均在事务中执行并回滚，无临时业务数据残留 |
| Java 17后端启动和数据源健康 | 条件通过 | Spring Boot、MyBatis映射和Hikari连接池初始化成功，健康端点返回HTTP 200；依赖临时TLS 1.0兼容且使用管理员账号，不满足上线安全条件 |

## 3. 尚未通过的上线门禁

1. TLS未通过。当前实例仅能与验证客户端协商TLS 1.0；Java 17默认安全策略拒绝连接。为完成隔离测试，本次仅在临时验证进程中启用TLS 1.0兼容并使用`encrypt=false`，没有修改项目运行配置。上线前必须为Windows Server和SQL Server安装医院批准的更新、启用TLS 1.2并配置可信服务器证书，再以Java 17默认安全策略复验。
2. 本次迁移使用临时管理员凭证执行。仍需分别使用`platform_migration`和`platform_app`账号验证迁移与应用运行的最小权限；平台应用不得使用系统管理员账号。
3. 本次验证覆盖从V0300升级到V0400，尚未执行全新空库V0100至V0400迁移。
4. 尚未使用`platform_app`执行DEV-007 MyBatis真实查询写入、事务隔离和超时验证；当前只完成管理员账号下的应用启动、映射初始化、连接池建连和数据源健康检查。
5. 尚未执行备份、恢复、完整性检查以及Express环境下的计划任务验证。

## 4. 安全说明

验证记录不包含服务器地址、登录名对应口令或应用凭证。临时兼容配置和行为验证脚本位于构建输出目录，不纳入版本库，也不得作为生产启动参数使用。
